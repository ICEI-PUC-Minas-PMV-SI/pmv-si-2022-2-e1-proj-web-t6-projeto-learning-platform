<div class="d-flex">
    <div class="gr-plan-progress {{ternary this.complete "complete" ""}} frontend d-flex flex-column align-items-center">
        <div>
            <i class="bi bi-check-lg fs-4"></i>
        </div>
        <div class="flex-fill"></div>
    </div>
    <div class="ps-3 pb-4 flex-fill">
        <h5>{{this.title}}</h5>
        <p>{{this.description}}</p>
    </div>
    <div class="d-flex align-items-center">
        <a href="#" class="fs-3">
            <i class="bi bi-arrow-right"></i>
        </a>
    </div>
</div>
